#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v;

    // vector is dynamic array

    // cout<<v.size()<<endl;

    // vector <int>v(5);
    // vector <int> v(5,10);// size ind values

    // int arr[5]={0,1,2,3,4};
    // vector<int> v(arr,arr+5); array and array +array size

    vector<int>v = {0,1,2,4}; //
    for(int i =0; i < v.size(); i++){
        cout<<v[i]<<" ";
    };
    cout<<endl;
    return 0;
}